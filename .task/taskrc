# [Created by task 1.9.4 9/17/2011 17:06:33]
# Taskwarrior program configuration file.
# For more documentation, see http://taskwarrior.org or try 'man task', 'man task-faq',
# 'man task-tutorial', 'man task-color', 'man task-sync' or 'man taskrc'

# Here is an example of entries that use the default, override and blank values
#   variable=foo   -- By specifying a value, this overrides the default
#   variable=      -- By specifying no value, this means no default
#   #variable=foo  -- By commenting out the line, or deleting it, this uses the default

# Use the command 'task show' to see all defaults and overrides

# syntax
# assignment
# <name-of-configuration-variable>=<value-to-be-set>

# Files
# you can put those data files in Dropbox, so that you can share data file between different
# machines.
# -----------------------------------------------------------
# you also can use ssh with task's command "merge", "pull" and "push".
# $ man task-sync
# e.g.:
#       task merge ssh://user@myremotehost/.task/
#       task push ssh://user@myremotehost/.task/
data.location=/home/chris/.task
locking=on
# gc=on
# [ sync ]
# task merge/pull/push [<URI>]
#merge.default.uri=
#push.default.uri=
#pull.default.uri=
#merge.autopush=yes|no|ask
merge.autopush=ask
# [ backup ]
# $ task push ~/backup/

# nesting configuration files
# include <path/to/the/configuration/file/to/be/included>

# Color theme (uncomment one to use)
#include /usr/local/share/doc/task/rc/light-16.theme
#include /usr/local/share/doc/task/rc/light-256.theme
#include /usr/local/share/doc/task/rc/dark-16.theme
#include /usr/local/share/doc/task/rc/dark-256.theme
#include /usr/local/share/doc/task/rc/dark-red-256.theme
#include /usr/local/share/doc/task/rc/dark-green-256.theme
#include /usr/local/share/doc/task/rc/dark-blue-256.theme
#include /usr/local/share/doc/task/rc/dark-violets-256.theme
#include /usr/local/share/doc/task/rc/dark-yellow-green.theme
include /usr/share/doc/task/rc/dark-256.theme
# =>
# $ task color legend
# theme color format
# 256-color support: color255
# RGB color support: rgb050

# TERMINAL
# for text wrapping when establish the size of window.
curses=on
defaultwidth=80
editor=vim
edit.verbose=on

# MISCELLANEOUS
locale=en-US
verbose=yes
# ask before deleting.
confirmation=yes
echo.command=yes
# comment.
annotations=full
report.X.annotations=full
# the number of tasks for each project that are shown in the task next command.
next=2
# when more than this number of tasks are modified in a single command, comfirmation will be
# required.
bulk=2
nag=You have higher priority tasks.
# whether the Tab completion scripts consider all the project names you have used, or just the ones used in active tasks.
# default=no
complete.all.projects=yes
# whether 'projects' commands lists all the project names you have used, or just ....
# default=no
list.all.projects=yes
complete.all.tags=yes
list.all.tags=yes
search.case.sensitive=no
xterm.title=on
# shuts off color automatically when the output is not sent directly to a TTY,
# e.g. $ task list > file
# available colors:
#       black, red, blue, green, magenta, cyan, yellow, white.
#       bold, bright, underline, inverse,
color=on
# keep color when redirect to file like: $ task list > file.txt,
# then you can show the color with : $ cat file.txt
_forcecolor=on

# foreground color & background color format:
#       bold green on bright red

# Turning this value off causes taskwarrior to generate a more vertically compact output.
blanklines=no
shell.prompt=task >>
# the character or string to show in the active column.
active.indicator=*
# the character or string to show in the tag_indicator column.
tag.indicator=+
# the character or string to show in the recurrence_indicator column.
recurrence.indicator=R
# the number of future recurring tasks to show.
# For example, if a  weekly recurring  task  is added with a due date of tomorrow, and
# recurrence.limit is set to 2, then a report will list 2 pending recurring tasks, one for tomorrow,
# and one for a  week from tomorrow.
recurrence.limit=1
# when the 'undo' command is run, task presents a before and after comparison of the data.
# this can be in either the 'slide' style. or 'diff' style.
undo.style=side # or =diff
burndown.bias=0.66
debug=off

# [[ alias ]]
alias.rm=delete

# DATES
dateformat=a D b Y
dateformat.report=M-D-Y
dateformat.holiday=YMD
dateformat.annotation=a D b Y
report.X.dateformat=a D b Y (vV) H:N:S
weekstart=Monday
displayweeknumber=yes
# the number of days into the future that define when a task is considered due, and is colored
# accordingly.
due=7
# color corresponding days in calendar.
calendar.details=sparse
calendar.details.report=list
calendar.offset=off
calendar.offset.value=-1
calendar.holidays=full

# Journal entries
journal.time=no
journal.time.start.annotation=Started task
journal.time.stop.annotation=Stopped task
journal.info=on

# Holidays
include /usr/share/doc/task/rc/holidays.en-US.rc
# FIXME modify China holidays file.
# include /home/chris/.task/includes/holidays-CN.rc
monthsperline=3

# DEPENDENCIES
# Determines whether dependency chain violations generate reminders.
dependency.reminder=on
# Determines whether dependency chain repair requires confirmation.
dependency.confirm=yes

# COLOR CONTROLS
color=on
fontunderline=on
# .......

# SHADOW FILE
shodow.file=$HOME/.task/shadow.txt
shadow.command=list
shadow.notify=on

# DEFAULTS
# default.project=code
# default.priority=M # default is blank
# default.due= # default is blank.
default.command=list
# default.command=list project:code

# REPORTS

