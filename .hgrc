[merge-tools]
# for Splice Vim plugin.
splice.executable = vim
splice.args = -f $base $local $other $output -c 'SpliceInit'
splice.premerge = keep
splice.priority = 1

[extensions]
# for zsh hg prompt.
# prompt = ~/.zsh/custom/hg-prompt/prompt.py
